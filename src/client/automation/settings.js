export const MOVING_SPEED = 1000; //pixels/ms
export const MOVING_SPEED_IN_DRAGGING = 4; //pixels/ms
export const MINIMUM_MOVING_TIME = 25;

//NOTE: some scripts work incorrectly when a delay between user actions too small, so we should make it more
export const ACTION_STEP_DELAY      = 60;
export const DRAG_ACTION_STEP_DELAY = 100;
