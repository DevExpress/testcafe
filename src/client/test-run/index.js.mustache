(function () {
    if (window.self !== window.top)
        return;


    var ClientDriver = window['%testCafeDriver%'];
    var driver = new ClientDriver('{{{testRunId}}}', {{{selectorTimeout}}}, '{{{browserHeartbeatUrl}}}', '{{{browserStatusUrl}}}');

    driver.start();
})();
