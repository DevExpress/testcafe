(function () {
    if (window.self !== window.top)
        return;


    var ClientDriver = window['%testCafeClientDriver%'];
    var driver = new ClientDriver('{{{testRunId}}}', '{{{browserHeartbeatUrl}}}', '{{{browserStatusUrl}}}');

    driver.start();
})();
