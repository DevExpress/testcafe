(function () {
    if (window !== window.top)
        return;


    var ClientDriver = window['%testCafeDriver%'];
    var driver = new ClientDriver('{{{testRunId}}}', {{{selectorTimeout}}}, '{{{browserHeartbeatUrl}}}', '{{{browserStatusUrl}}}', {{{initiallyExpectedDialogs}}});

    driver.start();
})();
