export default {
    'span category': () => '',

    'span step-name': str => `"${str}"`,

    'code': str => str,

    'strong': str => str,

    'code step-source': str => str,

    'a': str => `"${str}"`
};
