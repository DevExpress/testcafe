const ERROR_LINE_COLUMN_REGEXP   = /\[JS code\]:(\d+):(\d+)/;
const ERROR_LINE_OFFSET          = -1;
const ERROR_FILENAME             = '[JS code]';
const CALLSITE_RECORD_CLASS_NAME = 'CallsiteRecord';

export {
    ERROR_LINE_COLUMN_REGEXP,
    ERROR_LINE_OFFSET,
    ERROR_FILENAME,
    CALLSITE_RECORD_CLASS_NAME,
};
