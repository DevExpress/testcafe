'use strict';

var _interopRequireDefault = require('babel-runtime/helpers/interop-require-default').default;

exports.__esModule = true;
exports.getText = getText;

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _callsite = require('callsite');

var _callsite2 = _interopRequireDefault(_callsite);

exports.default = function (relativePath) {
    var caller = (0, _callsite2.default)()[1];
    var callerPath = caller.getFileName();
    var basePath = _path2.default.dirname(callerPath);
    var filePath = _path2.default.join(basePath, relativePath);

    return _fs2.default.readFileSync(filePath).toString();
};

var MESSAGES = {
    browserConnectionInterrupted: 'The {userAgent} browser did not respond to the TestCafe and was deleted. ' + 'This problem may appear when a browser hangs or is closed, or due to network issues.'
};

exports.MESSAGES = MESSAGES;

function getText(template) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        args[_key - 1] = arguments[_key];
    }

    return args.reduce(function (msg, arg) {
        return msg.replace(/{.+?}/, arg);
    }, template);
}
//# sourceMappingURL=../utils/read-file-relative.js.map