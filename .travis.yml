language: node_js
matrix:
 include:
  - node_js: "stable"
    env: GULP_TASK="test-functional-travis-ms-desktop"
 fast_finish: true

branches:
  except:
    - /^build-bot-temp-.*$/

notifications:
 email: false
