language: node_js
matrix:
 include:
  - node_js: "stable"
    env: env: GULP_TASK="test-functional-travis-legacy"
 fast_finish: true

branches:
  except:
    - /^build-bot-temp-.*$/

notifications:
 email: false
