language: node_js
matrix:
 include:
  - node_js: "stable"
    env: GULP_TASK="test-client-travis"
  - node_js: "stable"
    env: GULP_TASK="test-client-travis-mobile"
 fast_finish: true

branches:
  except:
    - /^build-bot-temp-.*$/

notifications:
 email: false
