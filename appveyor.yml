version: "#{build}"
clone_depth: 1

skip_commits:
  message: /^\[docs\]/

environment:
  GULP_TASK: "test-server"
  NODEJS_VERSION: "stable"

install:
- ps: >-
    Install-Product node $env:NODEJS_VERSION

- cmd: >-
    node --version

    npm --version

    npm install

build: off

